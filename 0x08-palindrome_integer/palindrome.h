#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int is_palindrome(unsigned long n);
unsigned long num_digit(unsigned long num);
char *reverse(char *str1);

#endif
